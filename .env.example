# Flask configuration
FLASK_APP=run.py
FLASK_ENV=development  # or production
SECRET_KEY=your-secret-key-here 
"""
Think of it like this: if your web app is a house, 
the secret key is like the master key that locks 
all the security features. Without it, you're basically 
leaving your doors unlocked!

NEVER SHARE YOUR SECRET_KEY!!!

"""

# Server configuration
PORT=5000
HOST=0.0.0.0

# File upload settings
MAX_CONTENT_LENGTH=16777216  # 16MB in bytes
UPLOAD_FOLDER=uploads

# Analysis parameters
CACHE_TIMEOUT=3600
MIN_SEQUENCE_LENGTH=20
MAX_SEQUENCE_LENGTH=200000

# Optional: RNA folding parameters
RNA_TEMP=37.0
RNA_MAX_LOOP=30
RNA_MAX_BP_SPAN=100

# Optional: Logging
LOG_LEVEL=INFO
LOG_FILE=crispradium.log